describe Displays all tasks in the global todos list.
function showTodos
    if todos is empty
        print "No tasks yet." with color "yellow"
        return
    end

    var index be 1

    for each task in todos
        print text from index + ". " + display of task
        set index to index + 1
    end
end
